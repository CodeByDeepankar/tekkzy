# serverless.yml
org: deepankar68
app: daily-spend-manager
service: daily-spend-manager

provider:
  name: aws
  runtime: nodejs20.x
  region: us-east-1
  environment:
    DYNAMODB_USERS_TABLE: ${self:service}-${sls:stage}-users
    DYNAMODB_CONTACTS_TABLE: ${self:service}-${sls:stage}-contacts
    DYNAMODB_SERVICE_REQUESTS_TABLE: ${self:service}-${sls:stage}-service-requests
    DYNAMODB_USERS_EMAIL_INDEX: email-index
    S3_BUCKET: ${self:service}-${sls:stage}-uploads-${aws:accountId}
    COGNITO_USER_POOL_ID:
      Ref: CognitoUserPool
    COGNITO_CLIENT_ID:
      Ref: CognitoUserPoolClient
    COGNITO_REGION: ${self:provider.region}
  iam:
    role:
      statements:
        - Effect: Allow
          Action:
            - dynamodb:PutItem
            - dynamodb:GetItem
            - dynamodb:Query
            - dynamodb:Scan
            - dynamodb:DeleteItem
            - dynamodb:UpdateItem
          Resource:
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-users
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-users/index/email-index
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-contacts
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-service-requests
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-service-requests/index/userId-index
            - arn:aws:dynamodb:${self:provider.region}:*:table/${self:service}-${sls:stage}-service-requests/index/status-index
        - Effect: Allow
          Action:
            - s3:PutObject
            - s3:GetObject
            - s3:DeleteObject
          Resource:
            - arn:aws:s3:::${self:service}-${sls:stage}-uploads-${aws:accountId}/*
        - Effect: Allow
          Action:
            - cognito-idp:AdminGetUser
            - cognito-idp:AdminCreateUser
            - cognito-idp:AdminDeleteUser
            - cognito-idp:AdminAddUserToGroup
            - cognito-idp:AdminListGroupsForUser
          Resource:
            - Fn::GetAtt: [CognitoUserPool, Arn]

functions:
  api:
    handler: lambda.handler
    events:
      # Root
      - http:
          path: /
          method: GET
          cors: true

      # Auth routes
      - http:
          path: /api/auth/register
          method: POST
          cors: true
      - http:
          path: /api/auth/confirm
          method: POST
          cors: true
      - http:
          path: /api/auth/resend-code
          method: POST
          cors: true
      - http:
          path: /api/auth/login
          method: POST
          cors: true
      - http:
          path: /api/auth/forgot-password
          method: POST
          cors: true
      - http:
          path: /api/auth/confirm-forgot-password
          method: POST
          cors: true
      - http:
          path: /api/auth/me
          method: GET
          cors: true

      # Contact routes
      - http:
          path: /api/contacts
          method: GET
          cors: true
      - http:
          path: /api/contacts/mine
          method: GET
          cors: true
      - http:
          path: /api/contacts
          method: POST
          cors: true
      - http:
          path: /api/contacts/{id}
          method: PUT
          cors: true
      - http:
          path: /api/contacts/{id}
          method: DELETE
          cors: true

      # Upload routes
      - http:
          path: /api/uploads/presign
          method: POST
          cors: true

      # Service Request routes
      - http:
          path: /api/service-requests
          method: POST
          cors: true
      - http:
          path: /api/service-requests
          method: GET
          cors: true
      - http:
          path: /api/service-requests/mine
          method: GET
          cors: true
      - http:
          path: /api/service-requests/{id}
          method: GET
          cors: true
      - http:
          path: /api/service-requests/{id}/status
          method: PUT
          cors: true
      - http:
          path: /api/service-requests/{id}/response
          method: PUT
          cors: true
      - http:
          path: /api/service-requests/{id}/notes
          method: PUT
          cors: true
      - http:
          path: /api/service-requests/{id}
          method: DELETE
          cors: true

plugins:
  - serverless-offline

resources:
  Resources:
    UsersTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:service}-${sls:stage}-users
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: userId
            AttributeType: S
          - AttributeName: email
            AttributeType: S
        KeySchema:
          - AttributeName: userId
            KeyType: HASH
        GlobalSecondaryIndexes:
          - IndexName: email-index
            KeySchema:
              - AttributeName: email
                KeyType: HASH
            Projection:
              ProjectionType: ALL

    ContactsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:service}-${sls:stage}-contacts
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: contactId
            AttributeType: S
        KeySchema:
          - AttributeName: contactId
            KeyType: HASH

    UploadsBucket:
      Type: AWS::S3::Bucket
      Properties:
        BucketName: ${self:service}-${sls:stage}-uploads-${aws:accountId}
        CorsConfiguration:
          CorsRules:
            - AllowedOrigins:
                - '*'
              AllowedHeaders:
                - '*'
              AllowedMethods:
                - PUT
                - POST

    CognitoUserPool:
      Type: AWS::Cognito::UserPool
      Properties:
        UserPoolName: ${self:service}-${sls:stage}-user-pool
        UsernameAttributes:
          - email
        AutoVerifiedAttributes:
          - email
        Policies:
          PasswordPolicy:
            MinimumLength: 8
            RequireLowercase: true
            RequireNumbers: true
            RequireSymbols: false
            RequireUppercase: true
        Schema:
          - Name: name
            AttributeDataType: String
            Mutable: true
            Required: false
          - Name: email
            AttributeDataType: String
            Mutable: false
            Required: true

    CognitoAdminGroup:
      Type: AWS::Cognito::UserPoolGroup
      Properties:
        GroupName: admin
        Description: Administrator group with elevated permissions
        UserPoolId:
          Ref: CognitoUserPool
        Precedence: 0

    ServiceRequestsTable:
      Type: AWS::DynamoDB::Table
      Properties:
        TableName: ${self:service}-${sls:stage}-service-requests
        BillingMode: PAY_PER_REQUEST
        AttributeDefinitions:
          - AttributeName: requestId
            AttributeType: S
          - AttributeName: userId
            AttributeType: S
          - AttributeName: status
            AttributeType: S
          - AttributeName: createdAt
            AttributeType: S
        KeySchema:
          - AttributeName: requestId
            KeyType: HASH
        GlobalSecondaryIndexes:
          - IndexName: userId-index
            KeySchema:
              - AttributeName: userId
                KeyType: HASH
              - AttributeName: createdAt
                KeyType: RANGE
            Projection:
              ProjectionType: ALL
          - IndexName: status-index
            KeySchema:
              - AttributeName: status
                KeyType: HASH
              - AttributeName: createdAt
                KeyType: RANGE
            Projection:
              ProjectionType: ALL

    CognitoUserPoolClient:
      Type: AWS::Cognito::UserPoolClient
      Properties:
        ClientName: ${self:service}-${sls:stage}-client
        UserPoolId:
          Ref: CognitoUserPool
        GenerateSecret: false
        ExplicitAuthFlows:
          - ALLOW_USER_PASSWORD_AUTH
          - ALLOW_REFRESH_TOKEN_AUTH
          - ALLOW_USER_SRP_AUTH
        PreventUserExistenceErrors: ENABLED
        AccessTokenValidity: 1
        IdTokenValidity: 1
        RefreshTokenValidity: 30
        TokenValidityUnits:
          AccessToken: days
          IdToken: days
          RefreshToken: days

  Outputs:
    CognitoUserPoolId:
      Value:
        Ref: CognitoUserPool
      Export:
        Name: ${self:service}-${sls:stage}-UserPoolId
    CognitoUserPoolClientId:
      Value:
        Ref: CognitoUserPoolClient
      Export:
        Name: ${self:service}-${sls:stage}-UserPoolClientId
